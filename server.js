const express = require('express'); const cors = require('cors'); const { GoogleGenerativeAI } = require('@google/generative-ai'); require('dotenv').config(); const app = express(); // Configuración CORS permisiva para evitar bloqueos app.use(cors()); app.use(express.json()); // ============================================================== // 🟢 RUTA DE PRUEBA: Entra a tu-url.vercel.app/api/test para probar // ============================================================== app.get('/api/test', (req, res) => { res.json({ status: 'ok', message: '¡El backend en Vercel está funcionando perfectamente!', apiKeyConfigurada: !!process.env.GEMINI_API_KEY }); }); app.post('/api/analyze', async (req, res) => { try { const { prompt, systemInstruction } = req.body; if (!process.env.GEMINI_API_KEY) { console.error("Falta GEMINI_API_KEY en las variables de entorno."); return res.status(500).json({ error: 'La API Key de Gemini no está configurada en Vercel.' }); } const genAI = new GoogleGenerativeAI(process.env.GEMINI_API_KEY); const model = genAI.getGenerativeModel({ model: "gemini-1.5-flash", systemInstruction: systemInstruction }); const result = await model.generateContent(prompt); const response = await result.response; res.json({ text: response.text() }); } catch (error) { console.error('Error detallado de IA:', error); res.status(500).json({ error: error.message || 'Error procesando la solicitud con Gemini.' }); } }); module.exports = app; 
